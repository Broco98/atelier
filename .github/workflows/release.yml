name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-latest # arm64 (aarch64-apple-darwin)
    steps:
      - uses: actions/checkout@v4

      - name: 태그와 매니페스트 버전 일치 확인
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          CONF_VERSION="$(jq -r .version src-tauri/tauri.conf.json)"
          if [[ "$VERSION" != "$CONF_VERSION" ]]; then
            echo "태그 v$VERSION 와 tauri.conf.json 버전 $CONF_VERSION 이 다릅니다." >&2
            echo "scripts/bump-version.sh 로 버전을 올린 커밋에 태그를 붙이세요." >&2
            exit 1
          fi
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: 의존성 설치
        run: pnpm install --frozen-lockfile

      - name: 테스트
        run: cargo test

      - name: 앱 빌드 (.dmg)
        run: pnpm tauri build

      - name: CLI 빌드 및 패키징
        run: |
          cargo build --release -p atelier-cli
          tar -czf "atelier-cli-${VERSION}-aarch64-apple-darwin.tar.gz" \
            -C target/release atelier

      - name: 앱 패키징 (tar.gz)
        run: |
          tar -czf "atelier-app-${VERSION}-aarch64-apple-darwin.tar.gz" \
            -C target/release/bundle/macos atelier.app

      - name: 릴리스 생성
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cat > release-notes.md <<'EOF'
          ## 설치

          한 줄로 앱·CLI·Claude 스킬을 모두 설치:

          ```sh
          curl -fsSL https://github.com/Broco98/atelier/releases/latest/download/install.sh | bash
          ```

          이미 설치된 단계는 건너뛰고, 중간에 실패해도 다시 실행하면 이어서 완료됩니다. sudo 불필요.

          <details>
          <summary>수동 설치</summary>

          **앱** — dmg를 열어 `atelier.app`을 Applications로 복사한 뒤, 서명이 없어 Gatekeeper가 막으므로 한 번 실행:

          ```sh
          xattr -cr /Applications/atelier.app
          ```

          **바이너리** — 압축을 풀어 PATH에 놓고 MCP 서버를 등록:

          ```sh
          tar -xzf atelier-cli-*.tar.gz
          sudo mv atelier /usr/local/bin/
          atelier mcp install
          ```

          </details>
          EOF
          gh release create "$GITHUB_REF_NAME" \
            --title "atelier v${VERSION}" \
            --notes-file release-notes.md \
            --generate-notes \
            target/release/bundle/dmg/*.dmg \
            "atelier-app-${VERSION}-aarch64-apple-darwin.tar.gz" \
            "atelier-cli-${VERSION}-aarch64-apple-darwin.tar.gz" \
            scripts/install.sh
