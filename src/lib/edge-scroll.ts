/**
 * 끄는 동안 **가장자리 자동 스크롤**의 수 — 사이드바 행 목록(`row-drop`의 `edgeScrollStep`)과 셸 탭 줄
 * (`tab-gap`의 `stripEdgeStep`)이 함께 쓴다. 한곳에 두는 것은 두 끌기의 손맛이 갈리지 않게 하려는 것이다 —
 * 두 파일에 같은 수를 따로 적으면 한쪽만 고쳐도 두 검사가 다 초록이다.
 *
 * 걸음 모양(띠를 상자 절반으로 좁히는가 · 상자 밖에서도 구르는가)은 두 자리가 일부러 다르다. 같은 것은 이 둘뿐이다.
 */

/** 자동 스크롤이 도는 가장자리 띠의 두께(px) — 행(55px)의 절반쯤이라 행 위를 지나는 손엔 안 걸린다. */
export const EDGE_BAND = 28;
/** 한 프레임에 가장 많이 굴리는 양(px). 60fps에 초당 600px — 목록 한 화면을 1초 안에 넘긴다. */
export const EDGE_MAX_STEP = 10;
