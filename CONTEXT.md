# Atelier

로컬 개발 작업을 조직하는 데스크톱 앱. 이 문서는 **말의 정본**이다 — 규격·구조·구현은
여기 적지 않는다.

## 세계

**모드**:
앱이 어느 세계를 보고 있는가. `Atelier`와 `Maison` **둘뿐이고 늘어나지 않는다** — 고를
것들의 목록이 아니라 축이라, 두 세계가 같은 화면으로 다른 디렉터리를 읽는다.
_주소가 정본이다_: `/maison/...`이면 Maison이고 아니면 Atelier다. 설정 밖에서는 사이드바
최상단 세그먼트가 그것을 말하고 그것으로 건너간다 — 「지금 어느 모드인가」를 앱이 따로 기억해
두고 그 기억이 주소와 어긋나는 판은 없다.
_설정은 어느 모드에도 안 속한다._ 두 세계가 한 `settings.json`을 쓰고 `/settings`에는
접두사가 안 붙는다 — 설정에는 세그먼트가 없고, 설정이 지니는 모드는 **떠나온 모드 하나다**
(「앱으로 돌아가기」가 그리로 간다).
_피할 말_: 공간, 워크스페이스

**Room**:
Maison의 상주 목록에 서는 하나. Atelier의 work과 **같은 규격에 다른 것**이다 — Room은
토픽이고 저장소에 안 붙어서 브랜치도 워크트리도 프로젝트도 없다.
세는 말은 「Room 3개」다. 목록 머리는 `Rooms`이지만 그것은 라벨이고, 문장에서는 이 말을 쓴다.
_work을 이 말로 부르지 않고, Room을 work이라 부르지도 않는다._ 둘을 한 낱말로 묶으면
「이 세계에는 프로젝트가 없다」 같은 규칙이 어느 쪽 이야기인지 문장에서 사라진다.

## 화면과 프로세스

**터미널**:
화면. work 화면이 보여 주는 쪽 하나이거나, main nav `Terminal`이 가는 곳이다.
그 안에 셸이 여럿 있다.
_이 말로 프로세스를 가리키지 않는다._

**셸**:
프로세스 하나. PTY 하나 · xterm 인스턴스 하나 · 탭 하나가 일대일이다.
사용자에게 세는 단위이기도 하다 — 「셸 8개」.
_피할 말_: 세션, 터미널, 탭

**명령**:
셸 안에서 도는 프로세스. 셸 자신이 아니다 — 빈 프롬프트에는 명령이 없다.
셸을 닫기 전에 「도는 것이 있는지」를 묻는 자리가 이 말을 쓴다.
_이 말로 앱이 백엔드에 부르는 것을 가리키지 않는다._
_막는 것은 여기까지다 — `commands.rs`의 「Tauri 명령」이나 CLI의 「명령」처럼 이미 자리를 잡은
용법은 다른 말이고 그대로 둔다. 새로 쓰는 사용자용 문구에서만 이 뜻이다._

**세션**:
`claude`·`codex`가 **자기 쪽에** 저장하는 대화. **앱의 개념이 아니다** — 앱은 세션을
만들지도 세지도 않는다.
_셸을 이 말로 부르지 않는다._ 막는 것은 여기까지다 — `sessionStorage`나 `pty 세션`처럼
수명을 가리키는 기존 용법은 다른 말이고 그대로 둔다.

**탭**:
work·터미널 화면 머리행의 한 칸. `spec`과 셸들이 거기 선다. 탭은 **화면 위의 자리**이고
셸은 **프로세스**다 — 한 셸이 한 탭에 서지만 둘은 다른 것이라, 세는 말은 「셸 8개」이지
「탭 8개」가 아니다.
_작업 패널의 `spec｜info`는 **패널 탭**으로 수식해 가른다._

**모든 프로젝트**:
프로젝트가 둘 이상인 work에서 모든 워크트리를 한 단계 위에서 보는 셸의 자리. ⌘T가 늘 여기에
연다 — 셸 안에서 눌러도 그 셸의 프로젝트가 아니라 여기다. 이 자리의 셸 탭에는 프로젝트 앞말이
없다. 프로젝트가 하나거나 없는 work에는 이 자리가 따로 없다.
폴더 이름은 이름으로 쓰지 않는다 — 화면에는 옅은 경로로만 보인다.
_피할 말_: trees, 루트

**열**:
본문이 좌우로 갈렸을 때의 한쪽. 늘 둘이고, 왼쪽과 오른쪽에 각각 spec과 터미널이 선다.
_사이드바와 작업 패널은 열이 아니다._ 그쪽은 늘 그 자리에 있는 칸이고, 열은 갈랐을 때만
생긴다 — 그래서 열에는 닫는 `×`가 있고 저 둘에는 접는 토글이 있다.
_열을 이 말로 부르지 않는다_: 칸, 패널, 페인. 「칸」 자체는 이 문서가 쓰는 말이다 —
탭이 「머리행의 한 칸」이고 위 문장이 사이드바·작업 패널을 그렇게 부른다.

**분할**:
본문을 열 둘로 가르는 것. 켜고 끄는 상태이지 화면 이름이 아니다 — 「분할 화면으로 간다」가
아니라 「분할을 켠다」다.
_조합은 늘 `spec ▏터미널`이고 좌우만 바뀐다._

**알림 띠**:
부르는 셸을 모으는 사이드바 위 상자. 모으는 것은 둘이다 — 답을 기다리는 셸과, 끝났는데
아직 안 본 셸. 머리에 적히는 이름은 「알림」이고, 설정의 「알림」과 같은 사건을 같은 말로 부른다.
_상태 이름 「확인할 것」과 가른다._ 그것은 띠 안 한 줄이 말하는 상태이지 띠의 이름이 아니다 —
띠를 그 말로 부르면 「나를 기다림」 줄만 서 있어도 머리가 「확인할 것」이라 말한다.
「활성화」도 띠의 이름이 아니다 — 작업 상태 `active`와 부딪히고 동작 말투로 읽힌다.
토스트를 「알림」이라 부르지 않는다. 이 말은 셸이 나를 부르는 사건의 것이다.
_피할 말_: 확인할 것 띠, 활성화 띠

**셸 스크롤백**:
xterm.js가 들고 있는 지나간 줄들.

**TUI 자기 스크롤**:
`claude`·`codex`가 alt 스크린 안에서 스스로 그리는 것. 셸 스크롤백과 다르다.

## spec 폴더의 모양

**레이아웃**:
spec 폴더가 어떤 모양이어야 하는지를 적은 데이터. **모드마다 하나다** — 처음에는 코드 안의
내장본이 쓰이고, 고치면 `~/.atelier/layouts/<모드>/` 폴더가 생겨 내장본을 가린다. 에이전트가 받는
안내문과 `spec` 패널 탭의 트리가 같은 레이아웃에서 나온다. 설정 nav 항목 `spec 레이아웃`이 그것을 보인다.
_레이아웃을 템플릿이라 부르지 않는다._ 템플릿은 파일 하나의 본문 뼈대이고, 레이아웃은 폴더 전체의
모양이다. 열·분할이 정하는 화면의 모양과도 다른 말이다.
_피할 말_: 노드, 화면 배치

**항목**:
레이아웃을 이루는 파일·폴더의 자리 하나. 이름 틀(`decisions.md`, `{n}-{name}`)과 에이전트에게 가는
설명, 아이콘, 템플릿을 든다. 폴더 항목은 자식 항목을 갖는다.
spec 폴더 자신도 항목이다 — **맨 위 항목**. 이름 틀이 없고, 그 설명이 안내문 맨 위의 방침 문단이다.
_설정 nav의 줄은 「설정 nav 항목」으로 수식해 가른다_ — 「패널 탭」과 같은 방식이다.
_피할 말_: 노드, 루트

**템플릿**:
파일 항목에 붙는 본문 뼈대. 레이아웃 폴더 안의 파일 한 장이고, 에이전트는 그 파일을 만들기 전에
템플릿을 읽고 모양을 따른다. 레이아웃 전체를 이 말로 부르지 않는다.

**번호 묶음**:
이름 틀에 `{n}`이 든 항목 하나가 폴더 하나 안에서 맞은 파일·폴더의 모임. 번호 순으로 쌓이고 최신이
앞에 선다 — 판(`01-…`, `02-…`)이 그 예다. 판 폴더 안의 `{n}` 항목은 판 폴더마다 따로 묶음이 선다.
판을 쓰지 않는 레이아웃에는 그런 항목을 두지 않으면 된다.

## 표기

**고르는 것의 라벨은 소문자 영어**:
탭 줄과 패널 탭 — `spec` · `info`. 한 화면에서 「무엇을 볼까」를 고르는 것들이라 한
가족으로 읽혀야 한다.
_대문자는 다른 컨트롤의 것이다_: 세계의 이름(`Atelier`·`Maison`), main nav, 그리고 구획
머리(`Rooms`). 한 화면에 대문자 `Terminal`(사이드바에서 가는 곳)과
소문자 `spec`(탭 줄에서 고르는 것)이 함께 서는데, **대소문자가 그 층을 가른다.**
main nav를 한 벌로 적지 않는다 — Atelier는 `Projects`·`Terminal`·`Archive` 셋이고 Maison은
`Terminal`·`Archive` 둘이다(저쪽 세계에는 프로젝트가 없다). 세계의 이름이 그 위에 서지만
같은 대문자 층인 것은, 설정 밖에서는 세그먼트가 nav와 한 컬럼에 붙어 있어 층이 갈리면 그
자리에서 보이기 때문이다(설정에는 둘 다 없다).
_설정 nav 항목의 라벨은 한국어다_(`터미널`·`알림`·`에이전트 훅`·`spec 레이아웃`) — main nav 층도,
탭 줄·패널 탭 가족도 아니다. 문장에서는 「터미널 설정」으로 써서 화면 「터미널」과 가른다.
`spec 레이아웃`의 소문자 `spec`은 「spec 폴더」와 같은 쓰임이다.

**문장은 한국어**:
라벨이 영어여도 문장 안에서는 한국어를 쓴다 — 탭은 `spec`, 문장은 「spec 폴더」.
셸도 그렇게 산다(칸에 적히는 것은 셸 이름, 문장은 「셸 8개까지예요」).
세계도 그렇다: 세그먼트에 적히는 것은 `Atelier`·`Maison`이고 문장은 「Maison에서는 …」이며,
목록 머리는 `Rooms`인데 세는 말은 「Room 3개」다.
