# Atelier

로컬 개발 작업을 조직하는 데스크톱 앱. 이 문서는 **말의 정본**이다 — 규격·구조·구현은
여기 적지 않는다.

## 화면과 프로세스

**터미널**:
화면. Work 화면의 탭 하나, 또는 main nav 항목 하나. 그 안에 셸이 여럿 있다.
_이 말로 프로세스를 가리키지 않는다._

**셸**:
프로세스 하나. PTY 하나 · xterm 인스턴스 하나 · 탭 줄의 칸 하나가 일대일이다.
사용자에게 세는 단위이기도 하다 — 「셸 8개」.
_피할 말_: 세션, 터미널, 탭

**세션**:
`claude`·`codex`가 **자기 쪽에** 저장하는 대화. **앱의 개념이 아니다** — 앱은 세션을
만들지도 세지도 않는다.
_셸을 이 말로 부르지 않는다._ 막는 것은 여기까지다 — `sessionStorage`나 `pty 세션`처럼
수명을 가리키는 기존 용법은 다른 말이고 그대로 둔다.

**셸 스크롤백**:
xterm.js가 들고 있는 지나간 줄들.

**TUI 자기 스크롤**:
`claude`·`codex`가 alt 스크린 안에서 스스로 그리는 것. 셸 스크롤백과 다르다.

## 표기

**탭 라벨은 소문자 영어**:
헤더 뷰 탭과 패널 탭 — `spec` · `terminal` · `shell` · `info`. 둘은 같은 층에 나란히
서므로 한 가족으로 읽혀야 한다.
_대문자는 다른 컨트롤의 것이다_: main nav(`Projects`·`Terminal`·`Archive`)와 구획
머리(`Iterations`·`Documents`).

**문장은 한국어**:
라벨이 영어여도 문장 안에서는 한국어를 쓴다 — 탭은 `shell`, 문장은 「셸 8개까지예요」.
`spec`이 이미 그렇게 산다(탭은 `spec`, 문장은 「spec 폴더」).
